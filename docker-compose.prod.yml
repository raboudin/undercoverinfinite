services:
  postgres:
    image: postgres:18-alpine
    container_name: undercoverinfinite-prod-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    networks:
      - db
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    image: ${CI_REGISTRY_IMAGE}/api:${IMAGE_TAG}
    container_name: undercoverinfinite-prod-api
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?schema=public
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - proxy
      - db
    labels:
      - traefik.enable=true
      - traefik.docker.network=proxy
      - traefik.http.routers.undercoverinfinite-prod-api.rule=Host(`api.undercoverinfinite.com`)
      - traefik.http.routers.undercoverinfinite-prod-api.entrypoints=websecure
      - traefik.http.routers.undercoverinfinite-prod-api.tls=true
      - traefik.http.routers.undercoverinfinite-prod-api.tls.certresolver=le
      - traefik.http.services.undercoverinfinite-prod-api.loadbalancer.server.port=3000

  app:
    image: ${CI_REGISTRY_IMAGE}/app:${IMAGE_TAG}
    container_name: undercoverinfinite-prod-app
    restart: unless-stopped
    networks:
      - proxy
    labels:
      - traefik.enable=true
      - traefik.docker.network=proxy
      - traefik.http.routers.undercoverinfinite-prod-app.rule=Host(`undercoverinfinite.com`)
      - traefik.http.routers.undercoverinfinite-prod-app.entrypoints=websecure
      - traefik.http.routers.undercoverinfinite-prod-app.tls=true
      - traefik.http.routers.undercoverinfinite-prod-app.tls.certresolver=le
      - traefik.http.services.undercoverinfinite-prod-app.loadbalancer.server.port=3000

networks:
  proxy:
    external: true
  db:

volumes:
  postgres_data:
